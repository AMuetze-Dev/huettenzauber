# Dev-Frontend (Vite-Dev-Server, HMR, Code sofort im Container).
# Bei geaenderten Dependencies (package.json):
#   docker compose up -d --build --renew-anon-volumes frontend
docker compose up frontend --build -d
